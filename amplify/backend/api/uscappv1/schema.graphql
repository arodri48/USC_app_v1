type Store @model(subscriptions: null) {
  id: ID!
  goodsType: String
  storeName: String
  stateLocation: String
  website: String
  BLM: Boolean
  PricePoint: String
  COVID: String
  bio: String
  image: String
}

type urlClicked @model(subscriptions: null){
  id: ID!
  storeID: ID!
  store: Store @connection(fields: ["storeID"])
}


